fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
pydantic>=1.10,<2.0
openai>=1.55.3  # Required for AzureOpenAI compatibility, do not downgrade below 1.55.3
aiohttp==3.9.1
redis==5.0.1
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.0.1
azure-cognitiveservices-speech==1.33.0
azure-ai-textanalytics==5.3.0
azure-identity==1.15.0
# Azure inference requires azure-core >=1.30.0, so do not pin azure-core below that
azure-ai-inference==1.0.0b9
# Do NOT pin azure-core here; let pip resolve a compatible version
httpx==0.27.2  # Pin to 0.27.2 to avoid 'proxies' argument error 